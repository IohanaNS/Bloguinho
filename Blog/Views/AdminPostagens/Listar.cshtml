@model Blog.ViewModels.Admin.AdminPostagensListarViewModel
@using Blog.ViewModels.Admin

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Listagem de Etiquetas</h3>

        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                <i class="fas fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="card-body">
        <a class="btn btn-primary mb-3" href="/admin/autores/criar">Criar novo autor</a>

        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nome</th>
                    <th>Categoria</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (PostagemAdminPostagens postagem in @Model.Postagens)
                {
                    <tr>
                        <td>@postagem.Id</td>
                        <td>@postagem.Titulo</td>

                        <td>
                            <a class="btn btn-default btn-xs" href="/admin/etiquetas/editar/@postagem.Id">Editar</a>
                            <a class="btn btn-danger btn-xs" href="/admin/etiquetas/remover/@postagem.Id">Remover</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>